This folder is used as a holding space for node modules that are compatible with the architecture/platform of the image we're running the back-end on.  It's mapped so we don't have to re-download a pile of modules every time we restart scicatlive.